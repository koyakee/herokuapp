<p><span><%= @rooms.count %></span>件見つかりました</p>

<%= @rooms.each do |room| %>
  <tr>
    <td><%= room.facilityImages %></td>
    <td><%= room.facilityName %></td>
    <td><%= room.facilityAddress %></td>
    <td><%= room.facilityDetails %></td>
  </tr>
<% end %>